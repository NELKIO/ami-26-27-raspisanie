# Расписание 2611-1 / 266-2 · 1 семестр 26/27

Одностраничное расписание, тянет данные напрямую из общей таблицы
«Расписание ПМИ и ЭАД, 1 семестр 26/27» (выгрузка листа в zip → разбор HTML
с разворачиванием объединённых ячеек).

Живая загрузка работает только с нормального адреса (https/localhost):
у файла, открытого двойным кликом, `Origin: null`, и Google не отдаёт данные.
Поэтому страница живёт на GitHub Pages.

При недоступности таблицы — сохранённая копия из localStorage, затем встроенный снимок.
